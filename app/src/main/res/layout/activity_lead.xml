<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.feicui.edu.housekeeper.activity.LeadActivity">

    <!-- ViewPager控件 -->
    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </android.support.v4.view.ViewPager>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        >
        <!-- 第一个圆形指示器 -->
        <ImageView
            android:id="@+id/icon1"
            android:layout_width="@dimen/lead_icons_width"
            android:layout_height="@dimen/lead_icons_height"
            android:layout_margin="@dimen/lead_icons_margin"
            android:contentDescription="@string/lead_text_description1"
            android:src="@drawable/adware_style_default"
            />
        <!-- 第二个圆形指示器 -->
        <ImageView
            android:id="@+id/icon2"
            android:layout_width="@dimen/lead_icons_width"
            android:layout_height="@dimen/lead_icons_height"
            android:layout_margin="@dimen/lead_icons_margin"
            android:contentDescription="@string/lead_text_description2"
            android:src="@drawable/adware_style_default"
            />
        <!-- 第三个圆形指示器 -->
        <ImageView
            android:id="@+id/icon3"
            android:layout_width="@dimen/lead_icons_width"
            android:layout_height="@dimen/lead_icons_height"
            android:layout_margin="@dimen/lead_icons_margin"
            android:contentDescription="@string/lead_text_description3"
            android:src="@drawable/adware_style_default"
            />
    </LinearLayout>

    <!-- 文本框 -->
    <TextView
        android:id="@+id/tv_skip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="@dimen/lead_skip_margin_right"
        android:layout_marginTop="@dimen/lead_skip_margin_top"
        android:text="@string/lead_skiptext"
        android:textStyle="bold"
        />

</RelativeLayout>
